import { EchartDirective } from './echart.directive';

describe('EchartDirective', () => {
  it('should create an instance', () => {
    // const directive = new EchartDirective('echart');
    // expect(directive).toBeTruthy();
  });
});
